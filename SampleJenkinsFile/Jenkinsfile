pipeline {
    agent any
    stages {
        stage('one') 
		{
            steps 
			{
                sh 'mvn --version'
            }
        }
		
		stage('two')
		{
			steps 
			{
                sh 'mvn --version'
            }
		}
		stage('three')
		{
			steps 
			{
                sh 'mvn --version'
            }
		}
		
    }
}

